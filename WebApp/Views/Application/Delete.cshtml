@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model Entities.Dtos.ApplicationDtos.ApplicationDto

@{
    ViewData["Title"] = "Uygulama Sil";
}

<div class="text-left">
    <form asp-action="Delete" asp-controller="Application">
         <input type="hidden" asp-for="Id" />
        <div class="col-lg-12">
            <label asp-for="Name" class="col-lg-4 col-form-label text-lg-start"></label>
            <input type="text" asp-for="Name" maxlength="100" disabled class="form-control form-control-lg form-control-solid" autocomplete="off" />
        </div>
        <div class="col-lg-12">
            <label asp-for="IsNviActive" class="col-lg-4 col-form-label text-lg-start"></label>
            <input type="checkbox" asp-for="IsNviActive" disabled class="form-check-input mb-3 mb-lg-0" checked/>
        </div>
        <div class="col-lg-12 mt-4">
            <a asp-action="Index" asp-controller="Application" class="btn btn-outline-secondary">İptal</a>
            <button type="submit" class="btn btn-danger">Sil</button>
        </div>

    </form>
    @if (Model is not null && !string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="col-lg-12 alert alert-danger mt-3">
            @Model?.ErrorMessage
        </div>
    }
</div>